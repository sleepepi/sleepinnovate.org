- @title = "What's Next"

.page-box.last-slide.background-p50-grey.center-vertically-xs-sm
  .container
    .row
      .col-lg-4.col-buffer
      .col-lg-4.col-buffer
        - if current_user.next_event_title
          = render "cards/coming_soon", title: current_user.next_event_title
        - else
          = render "cards/all_events_completed"

    .text-center
      Back to
      = succeed "."do
        = link_to "dashboard", dashboard_path
