!!!
%html{ lang: "en" }
  %head
    %meta{ charset: "utf-8" }
    %meta{ content: "IE=Edge,chrome=1", http: { equiv: "X-UA-Compatible" } }
    %meta{ name: "viewport", content: "width=device-width, initial-scale=1, shrink-to-fit=no" }
    %meta{ name: "description", content: "SleepINNOVATE project website." }
    %meta{ name: "author", content: "Remo Mueller" }
    %meta{ name: "keywords", content: "sleep innovate, sleep research" }
    -# Don't cache any pages
    %meta{ name: "turbolinks-cache-control", content: "no-cache" }
    %title= [@title, ENV["website_name"]].compact.join(" - ").html_safe
    = render "layouts/head/favicons"
    = csrf_meta_tags
    = stylesheet_link_tag "application", media: "all", data: { turbolinks_track: "reload" }
    = javascript_include_tag "application", data: { turbolinks_track: "reload" }
    = javascript_tag "var root_url='#{request.script_name + '/'}';"
  %body#top.full-page-no-header-no-footer{ class: yield(:body_class) }
    = render "layouts/flash_bar"
    = yield :header
    = yield
    = yield :footer
