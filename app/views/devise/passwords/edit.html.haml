.page-box.last-slide.background-p50-grey
  .sign-up-form
    .sign-up-form-title
      Set your password

    = form_with model: resource, as: resource_name, url: password_path(resource_name), method: :put, local: true do |form|
      = form.hidden_field :reset_password_token

      .form-group
        = render "layouts/label_or_errors", form: form, object: resource, key: :password, key_name: "New password"
        = form.password_field :password, class: "form-control"
        - if @minimum_password_length
          %small.form-text.text-muted
            At least
            %strong= @minimum_password_length
            characters, including lowercase letters
            = succeed "," do
              %code a-z
            uppercase letters
            = succeed "," do
              %code A-Z
            and digits
            = succeed "." do
              %code 0-9
      .form-group
        = render "layouts/label_or_errors", form: form, object: resource, key: :password_confirmation, key_name: "Confirm new password"
        = form.password_field :password_confirmation, class: "form-control"

      - if resource.errors[:reset_password_token].present?
        = form.label :reset_password_token, "Reset token has expired.", class: "form-control-label"
        = link_to "Request a new reset token.", new_password_path(resource_name)

      .form-group.submit-btn-container
        = form.submit "Update my password", class: "btn btn-success btn-block"
