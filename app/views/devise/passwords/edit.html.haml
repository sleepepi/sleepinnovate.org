.page-box.last-box.d-flex.flex-column.justify-content-center.background-p50-grey
  .container
    .sign-up-form
      .sign-up-form-title
        Set your password

      = form_with model: resource, as: resource_name, url: password_path(resource_name), method: :put, local: true do |form|
        = form.hidden_field :reset_password_token

        = render "forms/vertical/password_field", form: form, object: resource, key: :password, key_name: "New password", autocomplete: "new-password"
        = render "forms/vertical/password_field", form: form, object: resource, key: :password_confirmation, key_name: "Confirm new password", autocomplete: "new-password"

        .form-group.submit-btn-container
          = form.submit "Change my password", class: "btn btn-success btn-block"
