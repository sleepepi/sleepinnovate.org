.page-box.last-box.background-p50-grey
  .sign-up-form
    .sign-up-form-title
      Resend unlock instructions

    = form_with model: resource, as: resource_name, url: unlock_path(resource_name), method: :post, local: true do |form|
      .form-group
        = render "layouts/label_or_errors", form: form, object: resource, key: :email
        = form.email_field :email, autofocus: true, class: "form-control form-control-danger", autocomplete: "off", autocorrect: "off", autocapitalize: "none", spellcheck: "false"

      .form-group.submit-btn-container
        = form.submit "Resend unlock instructions", class: "btn btn-success btn-block"

  .sign-up-form.text-center
    = render "devise/shared/links"
