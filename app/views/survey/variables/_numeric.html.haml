.form-group{ class: @json.dig("errors", "base").present? ? "has-danger" : nil }
  .input-group
    = render "survey/decoration/addon", addon: @json.dig("variable", "prepend")
    = form.text_field "response", class: "form-control form-control-danger", value: response
    = render "survey/decoration/addon", addon: @json.dig("variable", "units")
    = render "survey/decoration/addon", addon: @json.dig("variable", "append")
  = render "survey/decoration/errors"
  = render "survey/decoration/field_note"
