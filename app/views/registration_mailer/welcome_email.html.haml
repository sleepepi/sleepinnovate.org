%p{ style: p_style }
  Welcome to the
  = render "layouts/mailer/logo"
  portal! We truly value your support and thank you
  for your interest. Your participation will help us better understand how sleep
  influences health and well-being and hopefully improve sleep healthcare.
  - if @user.consented?
    We recommend you
    = link_to "download and keep a copy of the consent", "#{ENV["website_url"]}/consent", style: link_style
    for your records.

%p{ style: p_style }
  Please create a password through the link below:

%p{ style: center_style }
  = link_to "Create my password", "#{ENV["website_url"]}/settings/password/reset?reset_password_token=#{@token}", style: link_style

= render "layouts/mailer/technical_help"

%p{ style: p_style }
  Thanks again,
%p{ style: p_style }
  The
  = render "layouts/mailer/logo"
  Team
